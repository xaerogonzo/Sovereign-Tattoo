"""
api/ — domain-specific mixins that compose into SovereignApi (see ../sv_api.py).

Each module exposes a single *ApiMixin class. The composer in sv_api.py inherits
all of them to assemble the JS-facing API surface.
"""
